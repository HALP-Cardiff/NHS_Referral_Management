# Optional: comma-separated browser origins. If set, this list REPLACES built-in
# defaults (localhost + https://referral-nhs-management.netlify.app). Include
# every origin you need, e.g. deploy previews or a custom domain:
# CORS_ORIGIN=https://referral-nhs-management.netlify.app,https://deploy-preview-123--your-site.netlify.app,http://localhost:3000
